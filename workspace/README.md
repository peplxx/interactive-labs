# Workspace

Directory for storing any scripts/files needed for the practical part of workshop.

- This directory is bind-mounted at `/home/ubuntu` as per `docker-compose.yaml`, it should also be mounted to other apps as needed.
- The subdirectory `recordings` stores all asciinema-recorded sessions.
- The subdirectory `downloads` containes files downloaded from browser.
